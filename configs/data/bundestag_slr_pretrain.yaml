_target_: src.data.bundestag_slr_datamodule.BundestagSLRDataModule

batch_size: ${trainer.devices} # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 8
pin_memory: True
max_frame_seq_length: 96
dataset_source: "/data/1braach/bundestag_slr"

pre_processor:
  _target_: src.models.components.feature_extractor_dinov2.SignLanguageFeatureExtractor
  return_every_nth_element: 1 # approx. 12 fps