# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: rwth_phoenix_2014
  - override /model: sign_language_module

data:
  batch_size: 8

paths:
  log_dir: /data/1braach/logs/


model:
  net:
    _target_: src.models.components.sign_language_net.SignLanguageNet
    pretrained_model_name_or_path: "lukasbraach/spatiotemporal_rwth_signlang"